/*
Hàm CONCAT() sử dụng để nối hai hoặc nhiều chuỗi kí tự lại thành một chuỗi thống nhất.
Thứ tự nối: chuỗi str1 sau đó đến str2, str3, …
Cú pháp:
CONCAT(str1, str2, ...., strn)*/
SELECT CONCAT(FirstName,LastName) FROM Students